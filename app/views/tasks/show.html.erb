<h1><%= @task.title %></h1>

<p> Description: <%= @task.details %></p>

<%= link_to 'Back to Tasks', tasks_path, class: 'blue' %>
<%= link_to 'Remove this Task', task_path(@task),method: :delete, data: { confirm: "Are you sure?" } %>
<%= link_to 'Edit a Task', edit_task_path(@task), class: 'green' %>
